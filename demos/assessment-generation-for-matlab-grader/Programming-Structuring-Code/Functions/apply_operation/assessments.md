# MATLAB Grader setup: Apply an operation

Purpose: summative  
Complexity: moderate  
Submission type: Function

| Requirement / LO evidence | Grader Test Type | MATLAB Grader UI fields | Code to paste | Expected evidence | Optional feedback on incorrect submission | Traceability |
| --- | --- | --- | --- | --- | --- | --- |
| The function applies the supplied function handle to the supplied array. | Variable equals reference solution | Student variable or expression: `transformedValues` | — | The call block result equals the reference result. | The returned values must come from evaluating the input function handle with `values`. | LO: pass functions as arguments using function handles |

Set **Code to call your function** to the contents of `function_call.m`. Add one **Variable equals reference solution** assessment. Add transfer calls in MATLAB Grader with a different anonymous function and input array to detect a hard-coded operation.
